# Install runtime deps first so local test environments match the app.
-r requirements.txt

# Development and testing dependencies
ruff>=0.6.0
black>=24.0.0
isort>=5.13.0
pre-commit>=3.7.0
pytest>=8.3.0
pytest-cov>=5.0.0
httpx>=0.27.0
